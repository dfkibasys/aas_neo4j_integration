statements:
- statement: |
    MATCH (s:Submodel {id: $submodel.id})
    OPTIONAL MATCH (s)-[:HAS_SUBMODEL_ELEMENT|HAS_CHILD*]->(elem)
    DETACH DELETE s, elem;
  parameters:
    submodel:
      id: "{{ event.id }}"
